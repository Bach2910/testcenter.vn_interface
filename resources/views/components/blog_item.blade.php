<div class="item-details">
    <div class="card-body">
        <img href="{{route('blog_lean')}}" src="{{$img}}" alt="page">
        <a href="{{route('document')}}" type="button" class="link">{{$name}}</a>
        <a href="{{route('blog_lean')}}" class="card-title">{{ $title }}</a>
        <p class="card-text">{{ $desc }}</p>
    </div>
</div>
